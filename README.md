# poppeyBallutaFrontend


git clone <repo url>


sudo docker-compose build 
sudo docker-compose up -d

frontend running on localhost:3000;
backend running on localhost:8000;
db runing on port 27017;


